# Build output
/bin/
/dist/
/eyebrow

# Go
*.test
*.out
coverage.*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local lockfiles produced during manual testing
/eyebrowlock.json

# Content-addressed cache of approved file bytes (line-diff baselines, H1b);
# a local cache, not part of the signed lockfile
/.eyebrow/snapshots/

# Local-only working docs (specs/plans); never committed
docs/superpowers/
